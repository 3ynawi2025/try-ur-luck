-- ============================================================
-- جرب حظك — هجرة المصادقة (اسم مستخدم + كلمة مرور)
-- ============================================================
-- ملاحظة: هذا الملف لا يُنفَّذ تلقائيًا — ينفّذه الأب لاحقًا على Supabase.
--
-- يُضيف عمود auth_email لتخزين البريد الاصطناعي الذي أنشأه السيرفر
-- لكل حساب (u_<uuid>@guest.jarebhazzak.app)، حتى يعمل تسجيل الدخول
-- باسم المستخدم وكلمة المرور بعد إعادة التثبيت أو على جهاز آخر.
-- الحسابات القديمة ستبقى قيمتها NULL حتى تعيّن كلمة مرور من الإعدادات.

ALTER TABLE profiles ADD COLUMN IF NOT EXISTS auth_email text;
